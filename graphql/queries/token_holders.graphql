query TokenHoldersQuery(
  $query: TokenHoldersQueryInput!
  $sort_by: TokenHoldersSortByInput!
  $limit: Int
) {
  tokenholders(query: $query, sortBy: $sort_by, limit: $limit) {
    token
    supply
    owner
    symbol
    account {
      publicKey
    }
  }
}
